from lessons.Python_relative_imports_children_63.utils.common.file_operations import save_to_file, read_file
from lessons.Python_relative_imports_children_63.utils.find import find_in

print('2.', __name__)

# user_input_data = input('Please insert some text: ')
# user_select_file = input(
# 	'Please enter the name of the file (default=data.txt) to which the text you entered will be saved: '
# )
#
# save_to_file(user_input_data, user_select_file if user_select_file != '' else 'data.txt')
# read_file(user_select_file if user_select_file != '' else 'data.txt')
